# Demo videos

These MP4 files are generated from the real `Board` and `Engine` APIs.

Regenerate them from the repository root:

```bash
pip install -r requirements-demo.txt
python scripts/create_demo_videos.py
```

